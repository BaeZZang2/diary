# 마음결 — 작업 규칙

## 이 저장소

일기·루틴 기록 PWA. 파일은 다음이 전부다.

- `index.html` — 앱 전체 (HTML·CSS·JS 한 파일)
- `sw.js` — 서비스 워커 (오프라인 실행·캐시)
- `manifest.webmanifest`, 아이콘 3개

빌드 도구도 테스트도 없다. `index.html` 을 고치는 것이 곧 앱을 고치는 것이다.

## 변경할 때마다 지킬 것

**1. `main` 까지 반영한다**

배포는 `main` 을 본다. 작업 브랜치에만 올리면 앱은 그대로다.
작업 브랜치에 커밋한 뒤 `main` 에 병합·푸시하는 데까지가 한 건의 작업이다.

PR 은 사용자가 따로 요청할 때만 만든다. 변경이 크거나 먼저 눈으로 확인하고
싶다고 하면 그때 PR 을 열고 병합 여부를 확인받는다.

**2. 버전을 함께 올린다**

두 곳을 같이 올린다. 한쪽만 올리면 앱이 옛 화면을 계속 들고 있는다.

- `index.html` 의 `APP_VERSION` — 설정 화면 맨 아래에 보인다. 날짜도 그날로.
- `sw.js` 의 `CACHE` — 캐시 이름이 그대로면 예전에 받아둔 파일이 남는다.

커밋 제목 끝에 `(v26)` 처럼 버전을 적는 것이 이 저장소의 관례다.

**3. 확인 방법을 안내한다**

작업을 마치면 사용자가 앱에서 무엇을 보면 되는지 알려준다.
설정 화면 맨 아래 버전이 새 번호로 보이면 반영된 것이다.
이미 설치한 앱은 완전히 닫았다 다시 열어야 할 수 있다.

**4. 일기 데이터는 따로 백업해야 한다**

일기·사진·루틴 기록은 브라우저 IndexedDB(`maeumgyeol`) 에만 있다.
git 에는 올라가지 않으므로 코드를 되돌려도 데이터는 돌아오지 않고,
코드 작업 때문에 데이터가 지워지지도 않는다.

다만 저장 구조를 건드리는 변경(`K.*` 키, 저장 형식, 마이그레이션) 전에는
설정의 '백업 파일 내보내기' 를 먼저 받아두시라고 안내한다.

## 말투

사용자는 개발자가 아니다. 브랜치·머지·PR 같은 말을 그대로 쓰지 말고
무엇이 어떻게 되는지 풀어서 설명한다.

앱 안의 글도 같은 결이다. 짧고 부드러운 한국어 구어체를 쓴다.
코드 주석도 한국어로, 왜 그렇게 했는지를 적는다.
